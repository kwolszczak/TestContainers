#datasources:
#  default:
#    url: jdbc:tc:postgresql:latest:///actor?TC_INITSCRIPT=file:src/test/resources/init-actor.sql?TC_DAEMON=true
#    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
#   minimum-idle: 5
micronaut:
  server:
    #port: -1
    port: 8081
    context-path: /myapp  # Set your desired context path here
jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop
